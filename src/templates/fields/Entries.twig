{% set <FieldHandle> = entry.<FieldHandle>.all() %}

{% if <FieldHandle> is not empty %}

  <h3>{{ "<FieldName>"|t }}</h3>

  <ul class="<FieldClass>">
    {% for rel in <FieldHandle> %}
      <li><a href="/{{ rel.uri }}">{{ rel.title|t }}</a></li>
    {% endfor %}
  </ul>

{% endif %}
