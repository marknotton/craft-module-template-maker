{% set <FieldHandle> = entry.<FieldHandle>.all() %}

{% if <FieldHandle> is not empty %}

  <h3>{{ "<FieldName>"|t }}</h3>

  {% for block in <FieldHandle> %}

    {% set blocktype = block.type.handle %}

    {% switch blocktype %}
      <FieldContent>
    {% endswitch %}

  {% endfor %}

{% endif %}
